apiVersion: v1
kind: Service
metadata:
  name: {{ include "technical-interview.fullname" . }}
  labels:
    {{- include "technical-interview.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "technical-interview.selectorLabels" . | nindent 4 }}